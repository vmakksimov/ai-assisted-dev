# DevGuard AI backend — development/test dependencies.
# Install with: python -m pip install -r requirements-dev.txt
# (includes runtime deps via the -r line below)

-r requirements.txt

pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=6.0
httpx>=0.28
ruff>=0.8
mypy>=1.13
