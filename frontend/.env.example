# Copy to .env.local. Base URL the React app uses to reach the API.
# Matches the backend port (8009). For Docker, this is baked in via the compose build arg.
VITE_API_BASE_URL=http://localhost:8009/api/v1
